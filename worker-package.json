{
  "name": "openclaw-demo-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for OpenClaw demo system",
  "main": "demo_worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "hono": "^4.0.0"
  }
}