#!/usr/bin/env python3
"""
Lookup utility for accessing guidelines and documentation.
"""

import sys
import os

# Add the src directory to the path so we can import the lookup utility
sys.path.insert(0, os.path.join(os.path.dirname(__file__), 'src'))

from lookup.lookup_util import main

if __name__ == "__main__":
    main()