#!/usr/bin/env python3
"""
Project Tracker utility for managing tasks and roadmap items.
"""

import sys
import os

# Add the src directory to the path so we can import the pt utility
sys.path.insert(0, os.path.join(os.path.dirname(__file__), 'src'))

from pt.cli import main

if __name__ == "__main__":
    main()